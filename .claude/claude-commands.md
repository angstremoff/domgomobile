# Claude Code Правильные Команды

## 📋 **Основные команды Claude Code:**

### 🎯 **Управление моделями:**
- `/model` - выбор модели (НЕ `claude model list`)
- `/model list` - показать доступные модели
- `/model sonnet` - переключить на Sonnet

### 🔧 **Управление MCP:**
- `/mcp` - управление MCP серверами
- `/mcp list` - показать MCP серверы
- `/mcp add` - добавить MCP сервер
- `/mcp remove` - удалить MCP сервер

### ⚙️ **Настройки:**
- `/config` - настройки Claude Code
- `/permissions` - управление разрешениями
- `/settings` - параметры сессии

### 💾 **Память и контекст:**
- `/memory` - управление памятью
- `/context` - показать текущий контекст
- `/compact` - сжать контекст

### 🛠️ **Работа с проектом:**
- `/add-dir` - добавить директорию
- `/help` - показать справку
- `/doctor` - диагностика проблем

## 🚨 **Частые ошибки:**
- ❌ `claude model list` → ✅ `/model`
- ❌ `claude --status` → ✅ `/status` (если есть)
- ❌ `claude permissions` → ✅ `/permissions`

## 💡 **Правильное использование:**
- Всегда использовать `/` для slash команд
- НЕ вызывать `claude` изнутри Claude Code
- Использовать встроенные команды вместо CLI