# 🔑 GitHub Secrets - Инструкция

## ✅ ПРАВИЛЬНЫЙ КЛЮЧ НАЙДЕН!

Ключ с отпечатком `79:F2:78:51:74:...` найден. Это тот самый ключ, который требует Google Play.

## Добавьте секреты в GitHub

1. Зайдите в репозиторий: **Settings -> Secrets and variables -> Actions**
2. Создайте (или обновите) следующие секреты:

| Name | Value |
| :--- | :--- |
| `ANDROID_KEYSTORE_BASE64` | *(Содержимое файла `found-release.keystore.base64`)* |
| `RELEASE_KEYSTORE_PASSWORD` | `domgo2024release` |
| `RELEASE_KEY_ALIAS` | `domgo-release` |
| `RELEASE_KEY_PASSWORD` | `domgo2024release` |

## Как скопировать base64

Откройте файл `found-release.keystore.base64` в любом текстовом редакторе и скопируйте **всё** содержимое (это длинная строка без пробелов и переносов).

## Запуск сборки

1. Перейдите во вкладку **Actions** на GitHub
2. Выберите слева **Build Android Release**
3. Нажмите **Run workflow**

Через ~10 минут в артефактах сборки появится `app-release.aab` версии 1.0.1, готовый к загрузке в Google Play.
